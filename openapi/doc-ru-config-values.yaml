type: object
properties:
  logLevel:
    description: Уровень логирования для приложений модуля
  cephfsEnabled:
    description: Включить или отключить CSI драйвер CephFS. При установке значения false контроллер драйвера CephFS и ресурсы CSIDriver не будут развернуты.
  rbdEnabled:
    description: Включить или отключить CSI драйвер RBD. При установке значения false контроллер драйвера RBD и ресурсы CSIDriver не будут развернуты.
  cephfsProvisionerWorkers:
    description: Настройки для provisioner workers для драйвера CephFS (по умолчанию 10). Пожалуйста, не устанавливайте это значение, если вы не уверены, что делаете.
  rbd3ProvisionerWorkers:
    description: Настройки для provisioner workers для драйвера RBD (по умолчанию 10). Пожалуйста, не устанавливайте это значение, если вы не уверены, что делаете.
  msCrcData:
    description: |
      Включить или отключить проверку контрольных сумм CRC32C для данных в сообщениях Ceph messenger.

      ВНИМАНИЕ: не отключайте этот параметр, если вы полностью не понимаете возможные последствия. При установке значения `false` параметр `ms_crc_data` в ceph.conf будет отключён. Это снижает способность Ceph обнаруживать повреждение данных в сообщениях messenger и может привести к незаметному повреждению данных, нарушению их консистентности или потере данных.

      Рекомендуемое и безопасное значение — `true`.

      ПРИМЕЧАНИЕ: изменение значения применяется только к новым операциям Ceph-клиентов, выполняемым CSI-драйвером: создание новых томов, а также новые операции attach/mount. Уже смонтированные тома продолжат работать со старым значением `ms_crc_data` до тех пор, пока не будут физически размонтированы с ноды и смонтированы заново.
